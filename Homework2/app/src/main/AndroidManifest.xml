<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sharangirdhani.homework2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddExpenseActivity"
            android:label="@string/btn_add_expense" />
        <activity
            android:name=".EditExpenseActivity"
            android:label="@string/btn_edit_expense"/>
        <activity
            android:name=".DeleteExpenseActivity"
            android:label="@string/btn_delete_expense"/>
        <activity
            android:name=".ShowExpensesActivity"
            android:label="@string/btn_show_expenses"></activity>
    </application>

</manifest>